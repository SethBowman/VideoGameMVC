@model Game

<h3>@Model.Title Information</h3>

<table class="table">
    <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Genre</th>
        <th>Release Year</th>
        <th>Platform</th>      
    </tr>
    <tr>
        <td>@Model.Id</td>
        <td>@Model.Title</td>
        <td>@Model.Genre</td>
        <td>@Model.Release_Year</td>
        <td>@Model.Platform</td>       
    </tr>
</table>

<div style="display: block">
    <a href="/Games/UpdateGame/@Model.Id">Update this Game</a>
</div>

<div style="display: block">

    @using (Html.BeginForm("DeleteGame", "Games", "Post"))
    {
        <input type="hidden" asp-for="Id" value="@Model.Id" />
        <input type="submit" onclick="return confirm('Are you sure?')" value="Delete this Game" />
    }
</div>